<% content_for :contextMenu do %>
	<% if currentUser.canEdit("Networks") %>
		<li class="dropdown">
			<a href="#" class="dropdown-toggle" data-toggle="dropdown">
		      	<i class="fa fa-bars fa fa-large"></i>
			</a>
		  	<ul class="dropdown-menu">
		      	<li>
		      		<%= link_to '<i class="fa fa-plus"></i> Dodaj Mrežu'.html_safe, new_network_path, "data-no-turbolink" => false %>
		      	</li>
			</ul>
		</li>
	<% end %>
<% end %>
<br>
<div class="container">
	<%= render :partial => "index", :locals => {:network => @network} %>
</div>
<br>
