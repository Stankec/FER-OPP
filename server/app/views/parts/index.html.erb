<% content_for :contextMenu do %>
	<% if currentUser != nil && currentUser.canEdit("Parts") %>
		<li class="dropdown">
			<a href="#" class="dropdown-toggle" data-toggle="dropdown">
		      	<i class="fa fa-bars fa fa-large"></i>
			</a>
		  	<ul class="dropdown-menu">
		      	<li>
		      		<%= link_to '<i class="fa fa-plus"></i> Dodaj nove dijelove'.html_safe, new_part_path(), "data-no-turbolink" => false %>
		      	</li>
			</ul>
		</li>
	<% end %>
<% end %>

<div class="container">
	<h1>Dijelovi</h1>
	<div>
		<%= render :partial => "index", :locals => {:part => @part} %>
	</div>
</div>