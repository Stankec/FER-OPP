<% content_for :contextMenu do %>
	<% if currentUser.canEdit("Categories") %>
		<li class="dropdown">
			<a href="#" class="dropdown-toggle" data-toggle="dropdown">
		      	<i class="fa fa-bars fa fa-large"></i>
			</a>
		  	<ul class="dropdown-menu">
		      	<li>
		      		<%= link_to '<i class="fa fa-plus"></i> Dodaj Kategoriju'.html_safe, new_category_path %>
		      	</li>
			</ul>
		</li>
	<% end %>
<% end %>
<br>
<div class="container">
	<h1>Kategorije</h1>
	<%= render :partial => "index", :locals => {:category => @category } %>
</div>
<br>
