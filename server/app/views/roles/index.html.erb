<% content_for :contextMenu do %>
	<% if currentUser != nil && currentUser.canEdit("Roles") %>
		<li class="dropdown">
			<a href="#" class="dropdown-toggle" data-toggle="dropdown">
		      	<i class="fa fa-bars fa fa-large"></i>
			</a>
		  	<ul class="dropdown-menu">
		      	<li>
		      		<%= link_to '<i class="fa fa-plus"></i> Dodaj novu Ulogu'.html_safe, new_role_path(), "data-no-turbolink" => false %>
		      	</li>
			</ul>
		</li>
	<% end %>
<% end %>

<div class="container">
	<h1>Uloge</h1>
	<%= render :partial => "index", :locals => {:role => @role} %>
</div>
